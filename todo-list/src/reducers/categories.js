import * as constants from '../actions/constants';

const initialState = [];

export const categories = (state = initialState, action) => {
    switch(action.type) {

        default:
            return state;
    }
}